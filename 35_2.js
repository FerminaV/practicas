// let resultado = document.getElementById("resultado");
// let notas = document.getElementById(notas).value;

function resultados(notas) {
  if (notas >= 18) {
    resultado.textContent = "PODES VOTAR";
   
  } else  {
    resultado.textContent = "Sentímolo moito pero non podes votar";
  }
}
